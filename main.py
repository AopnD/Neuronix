# representation of a singel node with 3 inputs ( data we getr from other nodes\ training data)
# weights and biases pre determind for now.

# inputs = [1.2, 2.3, 3.4]
# weights = [0.7, -0.5, 1.25]
# bias = 2
#
# output = inputs[0]*weights[0] + inputs[1]*weights[1] + inputs[2]*weights[2] + bias
#
# print(output)